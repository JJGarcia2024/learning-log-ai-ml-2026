# Stage 7 on the aerospace track

Defer to `nep-coder` Phase 7 — Staging, which runs Section 5.1 steps 4–7
(grid/mesh convergence, solver convergence, parameter sensitivity, cross-code
comparison) before the commit, and its Commit Convention
(`feat(project07): ...` / `feat(topic-a): ...`) for the commit message
itself. The `## RESULT` block (WHY IT WORKS / PERFORMANCE / VALIDATION /
PORTFOLIO / NTP-NEP LINK) is written only after Phase 7 passes.

## What this file adds

After the commit, update `Board_for_2026-2035_Extended_Study_Timeline`:
- Mark the period's "Done?" cell YES
- If this was one of the P1–P17 portfolio items, increment "Projects
  shipped" and check off the matching gate in "Gates passed"

## Gate to advance

Run nep-coder's Phase 7 and commit convention in full, then update the
Timeline board's progress counters — a shipped project that isn't reflected
on the board hasn't moved the mission-progress numbers.
